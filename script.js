let pos = {
lat: (-45)
lon: 112
};


getISS();
let pos = {
    lat: (-45)
    lon: 112
    };
    <Document.getElementByID("lat").textContent = latitude;></Document.getElementByID>
    <Document.getElementByID("lon").textContent = longitude;></Document.getElementByID>

    console.log(pos);
    const api_url = (https://wheretheiss.at/)
async function getISS() {
    const response = away fetch('https://wheretheiss.at/') (const api_url);
    const data = await response.json();
    const { latitude,  longitude } = data;
    console.log (data);
    console.log (data.latitude);
    console.log (data.longitude);